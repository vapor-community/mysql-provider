machine:
  services:
    - mysql
dependencies:
  override:
    - eval "$(curl -sL https://apt.vapor.sh)"
    - apt-get install swift cmysql
test:
  override:
    - swift build
    - swift build -c release
    - swift test
