machine:
  services:
    - mysql
dependencies:
  override:
    - eval "$(curl -sL https://apt.vapor.sh)"
    - sudo apt-get install swift cmysql
test:
  override:
    - sudo swift build
    - sudo swift build -c release
    - sudo swift test
