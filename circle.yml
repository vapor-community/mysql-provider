machine:
  services:
    - mysql
dependencies:
  override:
    - eval "$(curl -sL https://apt.vapor.sh)"
    - sudo apt-get update
    - sudo apt-get install vapor cmysql
test:
  override:
    - swift build
    - swift build -c release
    - swift test
